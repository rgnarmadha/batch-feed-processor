spring:
  application:
    name: spring-batch-feed-processor
  
  datasource:
    url: jdbc:h2:mem:feeddb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;CACHE_SIZE=65536;LOCK_TIMEOUT=10000
    driver-class-name: org.h2.Driver
    username: sa
    password:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000
    
  h2:
    console:
      enabled: true
      path: /h2-console
      
  jpa:
    hibernate:
      ddl-auto: update
      jdbc:
        batch_size: 1000
        order_inserts: true
        order_updates: true
        batch_versioned_data: true
    show-sql: false
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        jdbc:
          batch_size: 1000
          batch_versioned_data: true
        order_inserts: true
        order_updates: true
        generate_statistics: false
    
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always
      table-prefix: BATCH_

# Feed Processing Configuration
feed:
  processor:
    input-directory: ${FEED_INPUT_DIR:./input}
    processed-directory: ${FEED_PROCESSED_DIR:./processed}
    error-directory: ${FEED_ERROR_DIR:./error}
    
    # Feed configurations
    feeds:
      - name: "customer_feed"
        file-pattern: "customer_*.csv"
        table-name: "customers"
        id-column: "customer_id"
        enabled: true
        
      - name: "product_feed"
        file-pattern: "product_*.csv"
        table-name: "products"
        id-column: "product_id"
        enabled: true
        
      - name: "order_feed"
        file-pattern: "order_*.csv"
        table-name: "orders"
        id-column: "order_id"
        enabled: true
        
      - name: "inventory_feed"
        file-pattern: "inventory_*.csv"
        table-name: "inventory"
        id-column: "item_id"
        enabled: true
        
      - name: "supplier_feed"
        file-pattern: "supplier_*.csv"
        table-name: "suppliers"
        id-column: "supplier_id"
        enabled: true
        
      - name: "category_feed"
        file-pattern: "category_*.csv"
        table-name: "categories"
        id-column: "category_id"
        enabled: true
        
      - name: "employee_feed"
        file-pattern: "employee_*.csv"
        table-name: "employees"
        id-column: "employee_id"
        enabled: true
        
      - name: "transaction_feed"
        file-pattern: "transaction_*.csv"
        table-name: "transactions"
        id-column: "transaction_id"
        enabled: true
        
      - name: "location_feed"
        file-pattern: "location_*.csv"
        table-name: "locations"
        id-column: "location_id"
        enabled: true

# Scheduling Configuration
scheduling:
  enabled: true
  cron: "0 0 2 * * ?" # Daily at 2 AM

# Logging
logging:
  level:
    com.example.feedprocessor: INFO
    org.springframework.batch: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
